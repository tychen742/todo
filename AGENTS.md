# Project Notes

This project uses **Expo SDK 54** intentionally. See `docs/ARCHITECTURE.md` for the reason (Expo Go on iPhone requires SDK 54).

- Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

See `docs/` for all project documentation.

While working, update SKILLS and docs/ and keep them synced with the project reality
